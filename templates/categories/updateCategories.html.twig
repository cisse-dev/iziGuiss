{% extends 'base.html.twig' %}
{% block title %}
	{{ parent()}}
	| Modifier
	{{categories.nom}}
{% endblock %}

{% block h1 %}
	Modifier
	{{categories.nom}}
{% endblock %}

{% block body %}

	<div class="row justify-content-end">
		<a href="{{ path('app_categories') }}" class="btn btn-info p-2 col-md-3">Retour</a>
	</div>

	<div class="col-md-4 mx-auto">
		{{ form_start(formCategories) }}

		<div class="col-md-10 mx-auto">
			{{ form_row(formCategories.nom) }}
		</div>


		{{ form_end(formCategories) }}
		
        {#<div class="row justify-content-center">
			<input type="submit" class="btn btn-success mt-3 col-md-6" value="Modifier">
		</div>#}
	</div>
{% endblock %}